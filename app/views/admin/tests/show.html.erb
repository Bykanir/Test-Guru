<h2><%= t('.header', title: @test.title, level: @test.level)%></h2>

<table>
  <tr>
    <th><%= t('.questions') %></th>
    <th colspan="2"><%= t('.actions') %></th>
  </tr> 

  <%= render partial: 'admin/questions/question', collection: @test.questions %>
</table><br>

<%= link_to  t('helpers.submit.test.back'), admin_tests_path %> |
<%= link_to  t('helpers.submit.test.edit'), edit_admin_test_path %> |
<%= link_to  t('helpers.submit.test.create_question'), new_admin_test_question_path(@test) %> 